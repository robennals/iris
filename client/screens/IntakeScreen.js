import React from 'react';
import { Text } from 'react-native';
import { ScreenContentScroll } from '../components/basics';

export function IntakeScreen({community}) {

    return (
        <ScreenContentScroll>
            <Text>Intake Screen</Text>
        </ScreenContentScroll>
    )
}